<div class="heyiam-portfolio circuit" data-render-version="2" data-template="circuit" data-username="{{ user.username }}">

  <h1 class="project-title">{{ user.displayName }}</h1>
  {% if user.bio != blank %}
  <p class="narrative-text">{{ user.bio }}</p>
  {% endif %}

  {%- comment -%} Stats {%- endcomment -%}
  <div class="stat-grid" role="list" aria-label="Portfolio statistics">
    <div class="stat-card" role="listitem">
      <div class="stat-card__label">Projects</div>
      <div class="stat-card__value">{{ projects.size }}</div>
    </div>
    <div class="stat-card" role="listitem">
      <div class="stat-card__label">Sessions</div>
      <div class="stat-card__value">{{ totalSessions }}</div>
    </div>
    <div class="stat-card" role="listitem">
      <div class="stat-card__label">Lines</div>
      <div class="stat-card__value">{{ totalLoc | formatLoc }}</div>
    </div>
  </div>

  {%- comment -%} Leverage: PCB trace {%- endcomment -%}
  {% if efficiencyMultiplier %}
  <div class="circuit-leverage-trace" role="status" aria-label="Leverage: {{ efficiencyMultiplier }}">
    <div class="circuit-leverage-trace__pad circuit-leverage-trace__pad--left">
      <span style="opacity: 0.5;">{{ totalDurationMinutes | formatDuration }}</span>
    </div>
    <div class="circuit-leverage-trace__line" aria-hidden="true">
      <span class="circuit-leverage-trace__multiplier" style="color: var(--accent, #a3e635);">{{ efficiencyMultiplier }}</span>
    </div>
    <div class="circuit-leverage-trace__pad circuit-leverage-trace__pad--right">
      <span style="color: var(--accent, #a3e635);">{{ totalAgentDurationMinutes | formatDuration }}</span>
    </div>
  </div>
  {% endif %}

  {% if projects.size > 0 %}
  <div class="card">
    <div class="section-header">
      <h3 class="section-header__title">Projects</h3>
    </div>
    <div class="session-grid">
      {% for project in projects %}
      <div class="session-card">
        <h4 class="session-card__title">{{ project.title }}</h4>
        <span class="session-card__meta">{{ project.totalSessions }} sessions &middot; {{ project.totalLoc | formatLoc }} lines</span>
      </div>
      {% endfor %}
    </div>
  </div>
  {% endif %}

  {% render 'partials/_footer', sessionBaseUrl: sessionBaseUrl %}

</div>
